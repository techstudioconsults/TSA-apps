# @workspace/typescript-config

Shared TypeScript configurations for the TSA monorepo.

## Usage

In your app's `tsconfig.json`:

```json
{
  "extends": "@workspace/typescript-config/nextjs.json"
}
```
