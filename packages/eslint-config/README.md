# @workspace/eslint-config

Shared ESLint configurations for the TSA monorepo.

## Usage

In your app's `eslint.config.js`:

```js
import { nextJsConfig } from "@workspace/eslint-config/next-js";

export default nextJsConfig;
```
